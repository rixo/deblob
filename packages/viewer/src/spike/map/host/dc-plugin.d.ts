// SPIKE (step 09) — delete with src/spike. The plugin's signature, for the
// product's vite.config.ts: dc-plugin.js is spike JavaScript.
import type { Plugin } from "vite"

export declare function dc(): Plugin

export default {
  include: ["src/**"],
  // the laundering shape, confessed: the root re-exports an adapter and the
  // field discloses it — the finding goes away, the disclosure goes public
  assembly: ["src/index.ts"],
}

export default {
  include: ["src/**"],
  // the shape surface exists for: designation waves the entry through
  // layer-in-path
  // and the assembly row — the exports facts still have to match
  assembly: ["src/index.ts"],
}

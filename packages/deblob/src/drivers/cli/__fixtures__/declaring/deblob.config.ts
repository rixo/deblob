export default {
  include: ["src/**"],
  // the shape surface exists for: designation waves the entry through rule 2
  // and the assembly row — the exports facts still have to match
  assembly: ["src/index.ts"],
}
